import subprocess

command = ['pip', 'install', 'PyTelegramBotAPI']
try:
    subprocess.run(command)
except:
    print('Error with:', command)

command = ['pip', 'install', 'youtube_dl']
try:
    subprocess.run(command)
except:
    print('Error with:', command)

command = ['pip', 'install', 'opencv-python']
try:
    subprocess.run(command)
except:
    print('Error with:', command)

command = ['pip', 'install', 'PyTelegramBotAPI']
try:
    subprocess.run(command)
except:
    print('Error with:', command)

command = ['pip', 'install', 'urllib3']
try:
    subprocess.run(command)
except:
    print('Error with:', command)



    